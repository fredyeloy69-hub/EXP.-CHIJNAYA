rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lectura abierta para el dashboard (podes restringir con auth despues si queres)
    match /{document=**} {
      allow read: if true;
      allow write: if false; // Solo el Admin SDK (backend) puede escribir
    }
  }
}
